1. Run tests
    ```bash
    echo "run tests"
    ```
2. Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done.
    ```bash
    ./minimal.sh
    ```
3. Submit the change
    ```bash
    git commit -a -m "📋 RUNTIME: CanarySuiteScheduling" -m "Executed blocked plan: waiting for CONTRACTS schema canary"
    ```
