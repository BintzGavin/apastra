---
title: "Schema Dependency Graph"
description: "Visual representation of cross-domain schema dependencies"
audience: "developers | platform-teams | agents"
last_verified: "2026-03-11"
source_files:
  - "docs/status/CONTRACTS.md"
  - "docs/status/RUNTIME.md"
  - "docs/status/EVALUATION.md"
  - "docs/status/GOVERNANCE.md"
---

# Schema Dependency Graph

This dashboard visualizes how different domains rely on schemas provided by the CONTRACTS domain and outputs generated by other domains.

## Overview of Dependencies

The core architecture treats `CONTRACTS` as the foundational domain providing schemas that other domains use for structured data validation. `EVALUATION` relies on these schemas to generate concrete reports, which `GOVERNANCE` then uses to evaluate policies.

```mermaid
graph TD
    %% Core Schemas
    C_PS[prompt-spec.schema.json]:::contracts
    C_DS[dataset-manifest.schema.json]:::contracts
    C_E[evaluator.schema.json]:::contracts
    C_S[suite.schema.json]:::contracts
    C_CM[consumption-manifest.schema.json]:::contracts
    C_RR[run-request.schema.json]:::contracts
    C_RA[run-artifact.schema.json]:::contracts
    C_HA[harness-adapter.schema.json]:::contracts

    %% Pending Schemas
    C_RP[regression-policy.schema.json]:::pending
    C_DT[delivery-target.schema.json]:::pending
    C_PR[promotion-record.schema.json]:::pending
    C_B[baseline.schema.json]:::pending
    E_RRS[regression-report.schema.json]:::pending

    %% Domains and Actions
    R_R[RUNTIME Resolver]:::runtime
    E_H[EVALUATION Harness]:::evaluation
    G_P[GOVERNANCE Policies]:::governance

    %% Connections
    C_CM --> R_R

    C_PS --> E_H
    C_DS --> E_H
    C_E --> E_H
    C_S --> E_H
    C_RR --> E_H
    C_HA --> E_H
    C_B -.->|Blocked| E_H

    E_H -->|Generates| C_RA
    E_H -.->|Generates| E_RRS

    C_RP -.->|Blocked| G_P
    C_DT -.->|Blocked| G_P
    C_PR -.->|Blocked| G_P
    E_RRS -.->|Blocked| G_P

    %% Styles
    classDef contracts fill:#d1e7dd,stroke:#0f5132,stroke-width:2px;
    classDef runtime fill:#cff4fc,stroke:#055160,stroke-width:2px;
    classDef evaluation fill:#fff3cd,stroke:#664d03,stroke-width:2px;
    classDef governance fill:#f8d7da,stroke:#842029,stroke-width:2px;
    classDef pending fill:#e2e3e5,stroke:#41464b,stroke-width:2px,stroke-dasharray: 5 5;
```

## Interface Details

### RUNTIME Domain Interfaces
The RUNTIME domain relies on the `consumption-manifest.schema.json` contract to validate app-side pinning and overrides during resolution.

### EVALUATION Domain Interfaces
The EVALUATION domain consumes almost all core models:
- Consumes `run-request.schema.json` to instruct execution.
- Consumes `harness-adapter.schema.json` to configure the execution environment.
- Validates the target assets against `prompt-spec.schema.json`, `suite.schema.json`, `dataset-manifest.schema.json`, and `evaluator.schema.json`.
- Yields a structured directory conforming to `run-artifact.schema.json`.
- *Blocked:* Needs `baseline.schema.json` to establish regression starting points.

### GOVERNANCE Domain Interfaces
The GOVERNANCE domain consumes policies and acts on evaluation outputs:
- *Blocked:* Needs `regression-policy.schema.json` to define gating logic.
- *Blocked:* Needs `regression-report.schema.json` (from EVALUATION) to make pass/fail decisions.
- *Blocked:* Needs `delivery-target.schema.json` and `promotion-record.schema.json` to handle approved rollouts.
