# Reference Adapter
This adapter demonstrates a minimal mock implementation. For a production-grade harness, you can plug in `promptfoo` to run evaluations against real providers.

1. Install promptfoo: `npm install -g promptfoo`
2. Map the run request payload to a promptfoo config.
3. Execute `promptfoo eval` and translate the results to Apastra artifact format.
